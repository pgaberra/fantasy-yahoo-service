package com.fantasy.yahoo.league.dto;

/** Where a league's draft stands, as Yahoo's {@code draft_status} reports it. */
public enum DraftStatus {
    PRE_DRAFT,
    IN_PROGRESS,
    FINISHED,
    UNKNOWN
}
